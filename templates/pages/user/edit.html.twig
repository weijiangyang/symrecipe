{% extends 'base.html.twig' %}

{% block title %}Symrecipe-user-edit{% endblock %}

{% block body %}
<div class="container">
<h1 class="mt-4">Edition du profile de l'utilisator</h1>
        {% for message in app.flashes('warning') %}
           <div class="alert alert-warning">
            {{ message }}
           </div> 
        {% endfor %}
    {{ form(form) }}

</div>
    
{% endblock %}
