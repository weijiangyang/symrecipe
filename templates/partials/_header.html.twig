<nav class="navbar navbar-expand-lg navbar-dark bg-primary">
    <div class="container-fluid">
        <a class="navbar-brand" href="#">Navbar</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarColor01"
            aria-controls="navbarColor01" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarColor01">
            <ul class="navbar-nav me-auto">
                <li class="nav-item">
                    <a class="nav-link active" href="{{ path('home_index')}}">Accueil
                        <span class="visually-hidden">(current)</span>
                    </a>
                </li>
                {% if app.user %}
                    <li class="nav-item">
                    <a class="nav-link " href="{{ path('ingredient_index')}}">Mes ingrédients
                      
                    </a>
                </li>
                <li class="nav-item">
                    <a class="nav-link " href="{{ path('recipe_index')}}">Mes recettes
                      
                    </a>
                </li>
                
                
                {% endif %}
                
                
                
            </ul>
           <div class="nav-item dropdown">
                
                    {% if app.user %}
                        <a class="nav-link dropdown-toggle" data-bs-toggle="dropdown" href="#" role="button" aria-haspopup="true" aria-expanded="false">{{ app.user.fullName}}</a>
                        <div class="dropdown-menu">
                        
                        <a class="dropdown-item" href="{{ path('user_edit',{'id':app.user.id}) }}">Modifier mon information</a>
                        <a class="dropdown-item" href="{{ path('user_password_edit',{'id':app.user.id}) }}">Modifier mon mot de passe</a>
                        <div class="dropdown-divider"></div>
                        <a class="dropdown-item" href="{{ path('security_logout') }}">Deconnexion</a>
                    {% else %}
                    <div class="d-flex">

                        <a class="dropdown-item mx-5" href="{{ path('security_login') }}">Connexion</a>
                   
                        <a class="dropdown-item" href="{{ path('security_registration') }}">Inscription</a>
                    </div>
                        
                        
                    {% endif %}
                    
                    
                </div>
            <div>
        </div>
    </div>
</nav>